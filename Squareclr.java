import java.awt.*;
import java.applet.*;
public class Squareclr extends Applet
{
  public void paint(Graphics g)
  {
   // g.setColor(Color.blue);
   //  g.drawRect(50, 80, 150, 100);
    g.setColor(Color.magenta);
    g.fillRect(150, 150, 150, 150);
  }
}